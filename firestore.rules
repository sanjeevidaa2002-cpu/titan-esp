rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default-deny catch-all modified for full development accessibility
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
